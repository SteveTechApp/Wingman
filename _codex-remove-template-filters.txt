Wingman change request: remove redundant Template page filters

Goal:
Clean up /wingman/templates by removing the redundant filter controls currently displayed above the template cards. The screenshot shows unnecessary filter text/chips such as "All", category chips, and compressed A-Z style filter labels. These do not help the workflow and should be removed.

Files likely affected:
- src/wingman2/pages/TemplatesPage.tsx
- any related template data/helper file only if filter state is defined separately
- src/wingman2/styles/wingman-style-stack.css only if there are now-unused template filter styles that should be hidden/cleaned

Required behaviour:
1. Keep the main Search Templates text input.
2. Remove the category filter chip row:
   - All
   - Corporate
   - Education
   - Hospitality
   - Retail
   - Public sector
   - Healthcare
   - House of worship
   - Leisure
   - Visitor attraction
   - Video wall
   - Control room
3. Remove any secondary filter row or label that displays compressed filter letters/text above the template grid.
4. Remove the visible "25 options" style count if it is part of the filter UI.
5. Keep the Template Library heading, but make it clean and compact.
6. Template cards should still render normally.
7. Search should still filter cards by room/application/product text.
8. Do not remove the left navigation.
9. Do not change routes.
10. Do not add a new CSS file.

Suggested final layout:
- Page hero
- Search Templates input
- Template Library compact heading
- Template card grid

Acceptance checks:
- /wingman/templates no longer shows category filter chips.
- /wingman/templates no longer shows compressed A-Z/filter text.
- Search input still works.
- Template cards still display.
- Build does not fail.

Run:
npm run typecheck
npm run build

Commit message:
Remove redundant template filters
