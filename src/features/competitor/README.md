# Compatibility Feature Folder

This folder is kept only for compatibility with older imports.

Do not add new feature code here.

Use these canonical homes instead:

- `src/features/compare/`: compare UI and legacy compare references.
- `src/services/competitorComparisonService.ts`: canonical comparison service.
- `src/competitor/`: competitor domain logic and local compare support.
