import { Outlet } from "react-router-dom";
import TopBar from "@/app/navigation/TopBar";
import MissionControlNav from "@/ui2/nav/MissionControlNav";
import FloatingGuru from "@/app/navigation/FloatingGuru";
import GuidanceStrip from "@/app/navigation/GuidanceStrip";

export default function AppShell() {
  return (
    <div
      style={{
        display: "flex",
        flexDirection: "column",
        height: "100vh",
        width: "100%",
        background:
          "radial-gradient(circle at top left, rgba(30,64,175,0.14), transparent 22%), linear-gradient(180deg, #09111d 0%, #0b1220 100%)",
        color: "white",
        overflow: "hidden",
      }}
    >
      <div
        style={{
          height: 86,
          flexShrink: 0,
          borderBottom: "1px solid rgba(255,255,255,0.07)",
          background: "rgba(7,14,24,0.74)",
          backdropFilter: "blur(18px)",
        }}
      >
        <TopBar />
        <GuidanceStrip />
      </div>

      <div
        style={{
          display: "flex",
          flex: 1,
          overflow: "hidden",
        }}
      >
        <div
          style={{
            width: 252,
            flexShrink: 0,
            borderRight: "1px solid rgba(255,255,255,0.07)",
            overflowY: "auto",
            background:
              "linear-gradient(180deg, rgba(9,17,30,0.96) 0%, rgba(10,19,33,0.92) 100%)",
          }}
        >
          <MissionControlNav />
        </div>

        <div
          style={{
            flex: 1,
            overflowY: "auto",
            padding: 24,
          }}
        >
          <Outlet />
        </div>
      </div>

      <FloatingGuru />
    </div>
  );
}